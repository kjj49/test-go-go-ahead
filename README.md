*Перед запуском сервиса необходимо создать config.yml в директории config и .env в корне проекта (значения можно оставить по умолчанию).*

* *Для запуска сервиса можно использовать:*

  * ***docker-compose up (-d)***
